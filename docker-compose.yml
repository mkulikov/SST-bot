services:
  bot:
    build: .
    container_name: sst-bot
    restart: unless-stopped
    volumes:
      - bot_data:/app/data
    environment:
      - DB_PATH=/app/data/bot.db
      - BOT_TOKEN=${BOT_TOKEN}
      - LOG_LEVEL=INFO
      - TZ=Asia/Tbilisi
      - REGION=Artvin
    env_file: .env
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
volumes:
  bot_data: